#include<stdio.h>
#include<string.h>
int main()
{
    int i,n;
    char str[20];
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        if(str[0]=='#' && str[1]=='\0')
            return 0;
        if(!strcmp)
    }
}

