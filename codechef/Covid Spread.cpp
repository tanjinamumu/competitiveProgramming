#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    long long int n, d, sum1,sum2,sum3;
    cin>>t;
    while(t--)
    {
        cin>>n>>d;
        if(n>d)
        {
            if(d<=10)
            {
                sum1=pow(2,d);
            }
            else if(d>10)
            {
                sum2=pow(2,10);
                sum3=sum2*
            }
        }
    }
}
